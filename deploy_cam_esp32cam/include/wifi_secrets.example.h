#pragma once

// Copy this file to include/wifi_secrets.h and fill in your Wi-Fi.
// The firmware still builds without wifi_secrets.h, but then it starts
// its own setup AP instead of joining your home network.

#define DEPLOY_CAM_WIFI_SSID "your-wifi-name"
#define DEPLOY_CAM_WIFI_PASS "your-wifi-password"

